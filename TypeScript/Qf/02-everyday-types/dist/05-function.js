"use strict";
function greet(name) {
    console.log('hello, ' + name.toUpperCase() + '!');
}
greet('Zane');
function getFavoriteNumber() {
    return 7;
}
const names = ['Zane', 'Tht', 'Lh'];
names.forEach(function (s) {
    console.log(s.toUpperCase);
});
names.forEach((s) => {
    console.log(s.toLowerCase);
});
